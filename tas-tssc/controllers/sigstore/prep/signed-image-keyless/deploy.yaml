---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: signed-image-keyless
  labels:
    app: signed-image-keyless
spec:
  replicas: 1
  selector:
    matchLabels:
      app: signed-image-keyless
  template:
    metadata:
      labels:
        app: signed-image-keyless
    spec:
      containers:
      - name: signed-image-keyless
        image: $IMAGE
        ports:
        - containerPort: 8080
          protocol: TCP
---
apiVersion: v1
kind: Service
metadata:
  name: signed-image-keyless
  labels:
    app: signed-image-keyless
spec:
  selector:
    app: signed-image-keyless
  ports:
  - port: 8080
    targetPort: 8080
    protocol: TCP
    name: http
  type: ClusterIP
---
apiVersion: route.openshift.io/v1
kind: Route
metadata:
  name: signed-image-keyless
  labels:
    app: signed-image-keyless
spec:
  to:
    kind: Service
    name: signed-image-keyless
  port:
    targetPort: http
  tls:
    termination: edge
    insecureEdgeTerminationPolicy: Redirect
