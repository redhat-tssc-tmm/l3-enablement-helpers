---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: signed-image-key
  labels:
    app: signed-image-key
spec:
  replicas: 1
  selector:
    matchLabels:
      app: signed-image-key
  template:
    metadata:
      labels:
        app: signed-image-key
    spec:
      containers:
      - name: signed-image-key
        image: $IMAGE
        ports:
        - containerPort: 8080
          protocol: TCP
---
apiVersion: v1
kind: Service
metadata:
  name: signed-image-key
  labels:
    app: signed-image-key
spec:
  selector:
    app: signed-image-key
  ports:
  - port: 8080
    targetPort: 8080
    protocol: TCP
    name: http
  type: ClusterIP
---
apiVersion: route.openshift.io/v1
kind: Route
metadata:
  name: signed-image-key
  labels:
    app: signed-image-key
spec:
  to:
    kind: Service
    name: signed-image-key
  port:
    targetPort: http
  tls:
    termination: edge
    insecureEdgeTerminationPolicy: Redirect
